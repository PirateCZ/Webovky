class task{
    constructor(taskId, description, priority, status){
       this.taskId = taskId
       this.description = description
       this.priority = priority
       this.status = status
    }
}